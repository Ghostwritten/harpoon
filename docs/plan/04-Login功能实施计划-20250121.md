---
name: Login 功能实施计划
overview: 为 hpn 添加 login 子命令，支持 Docker/Podman/Skopeo/Nerdctl 多运行时的统一认证管理，包括安全的密码输入方式、非安全连接支持，以及认证信息的持久化存储。
todos:
  - id: extend_runtime_interface
    content: 扩展 Runtime 接口：添加 Login 方法和 LoginOptions 结构体
    status: completed
  - id: create_auth_utils
    content: 创建认证文件管理工具：auth.go，实现认证文件的读写和路径管理
    status: completed
  - id: implement_docker_login
    content: 实现 Docker Login 方法：支持 --password-stdin，处理非安全连接警告
    status: completed
    dependencies:
      - extend_runtime_interface
  - id: implement_podman_login
    content: 实现 Podman Login 方法：支持 --tls-verify=false 用于非安全连接
    status: completed
    dependencies:
      - extend_runtime_interface
  - id: implement_skopeo_login
    content: 实现 Skopeo Login 方法：支持 --tls-verify=false 用于非安全连接
    status: completed
    dependencies:
      - extend_runtime_interface
  - id: implement_nerdctl_login
    content: 实现 Nerdctl Login 方法：支持 --password-stdin，处理非安全连接警告
    status: completed
    dependencies:
      - extend_runtime_interface
  - id: create_login_cmd
    content: 创建 Login 子命令：实现交互式输入、stdin 读取、环境变量支持
    status: completed
    dependencies:
      - extend_runtime_interface
      - create_auth_utils
  - id: add_auth_to_pull_push
    content: 在 Pull/Push 子命令中添加可选的认证参数（临时认证，不持久化）
    status: cancelled
    dependencies:
      - extend_runtime_interface
  - id: test_login_functionality
    content: 测试 Login 功能：多运行时、认证文件、兼容性、安全性
    status: completed
    dependencies:
      - create_login_cmd
      - implement_docker_login
      - implement_podman_login
      - implement_skopeo_login
      - implement_nerdctl_login
  - id: update_documentation
    content: 更新文档：README、快速开始指南、添加安全最佳实践
    status: completed
    dependencies:
      - create_login_cmd
---

# Login 功能实施计划

## 概述

为 hpn 添加 `login` 子命令，提供统一的容器镜像仓库认证接口，支持 Docker、Podman、Skopeo、Nerdctl 四种运行时。实现安全的密码输入方式、非安全连接支持，以及认证信息的持久化存储。

## 核心需求

### 1. 功能需求

- **统一登录接口**：`hpn login <registry>` 替代各运行时的独立登录命令
- **多运行时支持**：自动检测或手动指定运行时（docker/podman/skopeo/nerdctl）
- **安全密码输入**：支持交互式输入、stdin 读取、环境变量
- **非安全连接**：支持 HTTP 和自签名证书的私有仓库
- **认证持久化**：将认证信息写入对应运行时的配置文件

### 2. 各运行时认证机制分析

#### Docker

- **登录命令**：`docker login [OPTIONS] [SERVER]`
- **认证文件**：`~/.docker/config.json`
- **文件格式**：
  ```json
  {
    "auths": {
      "registry.example.com": {
        "auth": "base64(username:password)"
      }
    }
  }
  ```

- **特殊参数**：`--password-stdin`（从 stdin 读取密码）

#### Podman

- **登录命令**：`podman login [OPTIONS] [REGISTRY]`
- **认证文件**：
  - Rootless: `~/.config/containers/auth.json`
  - Root: `/run/containers/$UID/auth.json`
- **文件格式**：与 Docker 兼容的格式
- **特殊参数**：`--authfile`（指定认证文件路径）

#### Skopeo

- **登录命令**：`skopeo login [OPTIONS] REGISTRY`
- **认证文件**：
  - 优先使用：`~/.config/containers/auth.json`
  - 兼容：`~/.docker/config.json`
- **文件格式**：与 Docker 兼容
- **特殊参数**：`--tls-verify=false`（非安全连接）

#### Nerdctl

- **登录命令**：`nerdctl login [OPTIONS] [SERVER]`
- **认证文件**：
  - 默认：`~/.docker/config.json`（与 Docker 兼容）
  - 可配置：通过 `CONTAINERD_ADDRESS` 环境变量
- **文件格式**：与 Docker 完全兼容
- **特殊参数**：`--password-stdin`

## 实施计划

### 1. 扩展 Runtime 接口

**文件**: `internal/runtime/interface.go`

添加 `Login` 方法到 `ContainerRuntime` 接口：

```go
// LoginOptions contains options for login operations
type LoginOptions struct {
    Username      string // 用户名
    Password      string // 密码（从 stdin 或环境变量读取）
    PasswordStdin bool   // 是否从 stdin 读取密码
    Insecure      bool   // 是否允许非安全连接（HTTP 或自签名证书）
    Debug         bool   // 是否启用 debug 模式
}

// ContainerRuntime defines the interface for container runtime operations
type ContainerRuntime interface {
    // ... existing methods ...
    
    // Login logs in to a container registry
    Login(ctx context.Context, registry string, options LoginOptions) error
}
```

### 2. 实现各运行时的 Login 方法

#### 2.1 Docker Login 实现

**文件**: `internal/runtime/docker.go`

```go
func (d *DockerRuntime) Login(ctx context.Context, registry string, options LoginOptions) error {
    args := []string{"login"}
    
    if options.Username != "" {
        args = append(args, "-u", options.Username)
    }
    
    if options.PasswordStdin {
        args = append(args, "--password-stdin")
    } else if options.Password != "" {
        args = append(args, "-p", options.Password)
    }
    
    if options.Insecure {
        // Docker 不支持 --insecure，需要通过环境变量或配置
        // 对于 HTTP，需要配置 daemon.json
        // 对于自签名证书，需要将证书添加到 /etc/docker/certs.d/
        // 这里先记录警告
        fmt.Printf("Warning: Docker does not support --insecure flag directly. " +
                   "For HTTP registries, configure daemon.json. " +
                   "For self-signed certificates, add certificates to /etc/docker/certs.d/\n")
    }
    
    args = append(args, registry)
    
    cmd := exec.CommandContext(ctx, d.command, args...)
    
    // 如果使用 --password-stdin，从 stdin 读取密码
    if options.PasswordStdin && options.Password != "" {
        cmd.Stdin = strings.NewReader(options.Password)
    }
    
    // ... (error handling) ...
}
```

#### 2.2 Podman Login 实现

**文件**: `internal/runtime/podman.go`

```go
func (p *PodmanRuntime) Login(ctx context.Context, registry string, options LoginOptions) error {
    args := []string{"login"}
    
    if options.Username != "" {
        args = append(args, "-u", options.Username)
    }
    
    if options.PasswordStdin {
        args = append(args, "--password-stdin")
    } else if options.Password != "" {
        args = append(args, "-p", options.Password)
    }
    
    if options.Insecure {
        args = append(args, "--tls-verify=false")
    }
    
    args = append(args, registry)
    
    cmd := exec.CommandContext(ctx, p.command, args...)
    
    // ... (similar to Docker) ...
}
```

#### 2.3 Skopeo Login 实现

**文件**: `internal/runtime/skopeo.go`

```go
func (s *SkopeoRuntime) Login(ctx context.Context, registry string, options LoginOptions) error {
    args := []string{"login"}
    
    if options.Username != "" {
        args = append(args, "-u", options.Username)
    }
    
    if options.PasswordStdin {
        args = append(args, "--password-stdin")
    } else if options.Password != "" {
        args = append(args, "-p", options.Password)
    }
    
    if options.Insecure {
        args = append(args, "--tls-verify=false")
    }
    
    args = append(args, registry)
    
    cmd := exec.CommandContext(ctx, s.command, args...)
    
    // ... (similar to Podman) ...
}
```

#### 2.4 Nerdctl Login 实现

**文件**: `internal/runtime/nerdctl.go`

```go
func (n *NerdctlRuntime) Login(ctx context.Context, registry string, options LoginOptions) error {
    args := []string{"login"}
    
    if options.Username != "" {
        args = append(args, "-u", options.Username)
    }
    
    if options.PasswordStdin {
        args = append(args, "--password-stdin")
    } else if options.Password != "" {
        args = append(args, "-p", options.Password)
    }
    
    // Nerdctl 不支持 --insecure，但可以通过环境变量配置
    if options.Insecure {
        fmt.Printf("Warning: Nerdctl does not support --insecure flag directly. " +
                   "Configure containerd for insecure registries.\n")
    }
    
    args = append(args, registry)
    
    cmd := exec.CommandContext(ctx, n.command, args...)
    
    // ... (similar to Docker) ...
}
```

### 3. 创建 Login 子命令

**文件**: `cmd/hpn/login.go` (新建)

```go
package main

import (
    "bufio"
    "context"
    "fmt"
    "os"
    "syscall"
    "time"
    
    "golang.org/x/term"
    "github.com/spf13/cobra"
    containerruntime "github.com/harpoon/hpn/internal/runtime"
)

var (
    loginRegistry   string
    loginUsername   string
    loginPassword   string
    loginPasswordStdin bool
    loginInsecure   bool
)

var loginCmd = &cobra.Command{
    Use:   "login [REGISTRY]",
    Short: "Log in to a container registry",
    Long:  `Log in to a container registry using the selected runtime.`,
    Args:  cobra.MaximumNArgs(1),
    RunE:  executeLogin,
}

func init() {
    loginCmd.Flags().StringVarP(&loginUsername, "username", "u", "", "Registry username")
    loginCmd.Flags().StringVarP(&loginPassword, "password", "p", "", "Registry password (not recommended, use --password-stdin or interactive input)")
    loginCmd.Flags().BoolVar(&loginPasswordStdin, "password-stdin", false, "Read password from stdin")
    loginCmd.Flags().BoolVar(&loginInsecure, "insecure", false, "Allow connections to registries without TLS or with self-signed certificates")
    rootCmd.AddCommand(loginCmd)
}

func executeLogin(cmd *cobra.Command, args []string) error {
    // Get registry from args or flag
    if len(args) > 0 {
        loginRegistry = args[0]
    }
    
    if loginRegistry == "" {
        return fmt.Errorf("registry is required")
    }
    
    // Select container runtime
    selectedRuntime, err := selectContainerRuntime()
    if err != nil {
        return fmt.Errorf("container runtime selection failed: %v", err)
    }
    
    fmt.Printf("Logging in to %s using %s...\n", loginRegistry, selectedRuntime.Name())
    
    // Get username
    username := loginUsername
    if username == "" {
        username, err = promptUsername()
        if err != nil {
            return err
        }
    }
    
    // Get password
    password := loginPassword
    if loginPasswordStdin {
        // Read from stdin
        scanner := bufio.NewScanner(os.Stdin)
        if scanner.Scan() {
            password = scanner.Text()
        }
    } else if password == "" {
        // Interactive input
        password, err = promptPassword()
        if err != nil {
            return err
        }
    } else {
        // Password provided via -p flag, show warning
        fmt.Printf("Warning: Using --password flag is insecure. " +
                   "Password may appear in shell history. " +
                   "Consider using --password-stdin or interactive input.\n")
    }
    
    // Prepare login options
    loginOptions := containerruntime.LoginOptions{
        Username:      username,
        Password:      password,
        PasswordStdin: loginPasswordStdin,
        Insecure:      loginInsecure,
        Debug:         IsDebug(),
    }
    
    // Execute login
    ctx, cancel := context.WithTimeout(context.Background(), 30*time.Second)
    defer cancel()
    
    if err := selectedRuntime.Login(ctx, loginRegistry, loginOptions); err != nil {
        return fmt.Errorf("login failed: %v", err)
    }
    
    fmt.Printf("✅ Successfully logged in to %s\n", loginRegistry)
    return nil
}

// promptUsername prompts for username interactively
func promptUsername() (string, error) {
    fmt.Print("Username: ")
    scanner := bufio.NewScanner(os.Stdin)
    if !scanner.Scan() {
        return "", fmt.Errorf("failed to read username")
    }
    return strings.TrimSpace(scanner.Text()), nil
}

// promptPassword prompts for password interactively (hidden input)
func promptPassword() (string, error) {
    fmt.Print("Password: ")
    bytePassword, err := term.ReadPassword(int(syscall.Stdin))
    if err != nil {
        return "", fmt.Errorf("failed to read password: %v", err)
    }
    fmt.Println() // New line after password input
    return string(bytePassword), nil
}
```

### 4. 支持环境变量

**文件**: `cmd/hpn/login.go`

添加环境变量支持：

```go
// Check for environment variables
if loginUsername == "" {
    loginUsername = os.Getenv("REGISTRY_USERNAME")
}
if loginPassword == "" && !loginPasswordStdin {
    loginPassword = os.Getenv("REGISTRY_PASSWORD")
}
```

### 5. 认证文件管理工具

**文件**: `internal/runtime/auth.go` (新建)

创建认证文件管理工具函数：

```go
package runtime

import (
    "encoding/base64"
    "encoding/json"
    "fmt"
    "os"
    "path/filepath"
)

// AuthConfig represents Docker/Podman auth configuration
type AuthConfig struct {
    Auths map[string]AuthEntry `json:"auths"`
}

type AuthEntry struct {
    Auth string `json:"auth"` // base64(username:password)
}

// GetAuthFilePath returns the auth file path for the given runtime
func GetAuthFilePath(runtimeName string) (string, error) {
    homeDir, err := os.UserHomeDir()
    if err != nil {
        return "", err
    }
    
    switch runtimeName {
    case "docker", "nerdctl":
        return filepath.Join(homeDir, ".docker", "config.json"), nil
    case "podman", "skopeo":
        return filepath.Join(homeDir, ".config", "containers", "auth.json"), nil
    default:
        return "", fmt.Errorf("unknown runtime: %s", runtimeName)
    }
}

// ReadAuthConfig reads auth configuration from file
func ReadAuthConfig(authFile string) (*AuthConfig, error) {
    config := &AuthConfig{
        Auths: make(map[string]AuthEntry),
    }
    
    data, err := os.ReadFile(authFile)
    if err != nil {
        if os.IsNotExist(err) {
            return config, nil // Return empty config if file doesn't exist
        }
        return nil, err
    }
    
    if err := json.Unmarshal(data, config); err != nil {
        return nil, fmt.Errorf("failed to parse auth config: %v", err)
    }
    
    return config, nil
}

// WriteAuthConfig writes auth configuration to file
func WriteAuthConfig(authFile string, config *AuthConfig) error {
    // Ensure directory exists
    dir := filepath.Dir(authFile)
    if err := os.MkdirAll(dir, 0755); err != nil {
        return fmt.Errorf("failed to create auth directory: %v", err)
    }
    
    data, err := json.MarshalIndent(config, "", "  ")
    if err != nil {
        return fmt.Errorf("failed to marshal auth config: %v", err)
    }
    
    if err := os.WriteFile(authFile, data, 0600); err != nil {
        return fmt.Errorf("failed to write auth file: %v", err)
    }
    
    return nil
}

// EncodeAuth encodes username:password to base64
func EncodeAuth(username, password string) string {
    auth := username + ":" + password
    return base64.StdEncoding.EncodeToString([]byte(auth))
}
```

### 6. 在 Pull/Push 操作中支持认证参数

**文件**: `cmd/hpn/pull.go`, `cmd/hpn/push.go`

添加可选的认证参数（用于临时认证，不持久化）：

```go
// Pull 子命令添加参数
pullCmd.Flags().StringVarP(&pullUsername, "username", "u", "", "Registry username (for this operation only)")
pullCmd.Flags().StringVarP(&pullPassword, "password", "p", "", "Registry password (for this operation only, not recommended)")

// Push 子命令添加参数
pushCmd.Flags().StringVarP(&pushUsername, "username", "u", "", "Registry username (for this operation only)")
pushCmd.Flags().StringVarP(&pushPassword, "password", "p", "", "Registry password (for this operation only, not recommended)")
```

## 技术细节

### 1. 密码安全处理

**优先级**（从最安全到最不安全）：

1. **交互式输入**：使用 `term.ReadPassword` 隐藏输入
2. **stdin 读取**：`--password-stdin`，适合 CI/CD
3. **环境变量**：`REGISTRY_PASSWORD`
4. **命令行参数**：`-p password`（不推荐，显示警告）

### 2. 认证文件位置

| 运行时 | 认证文件路径 | 说明 |

|--------|------------|------|

| Docker | `~/.docker/config.json` | 标准位置 |

| Podman | `~/.config/containers/auth.json` | Rootless 模式 |

| Skopeo | `~/.config/containers/auth.json` | 优先使用 |

| Nerdctl | `~/.docker/config.json` | 与 Docker 兼容 |

### 3. 非安全连接处理

- **Docker**：不支持直接 `--insecure`，需要配置 daemon.json 或证书
- **Podman/Skopeo**：支持 `--tls-verify=false`
- **Nerdctl**：不支持直接参数，需要配置 containerd

### 4. 认证验证

在保存认证信息前，先验证凭据有效性：

- 调用 Registry V2 API: `GET /v2/`
- 使用 Basic Auth 进行验证
- 验证成功后再写入配置文件

## 文件清单

### 新建文件

- `cmd/hpn/login.go` - Login 子命令实现
- `internal/runtime/auth.go` - 认证文件管理工具

### 修改文件

- `internal/runtime/interface.go` - 添加 `Login` 方法和 `LoginOptions`
- `internal/runtime/docker.go` - 实现 `Login` 方法
- `internal/runtime/podman.go` - 实现 `Login` 方法
- `internal/runtime/skopeo.go` - 实现 `Login` 方法
- `internal/runtime/nerdctl.go` - 实现 `Login` 方法
- `cmd/hpn/pull.go` - 添加可选的认证参数（可选）
- `cmd/hpn/push.go` - 添加可选的认证参数（可选）

## 使用示例

### 基本用法

```bash
# 交互式登录（最安全）
hpn login harbor.company.com

# 使用参数登录
hpn login harbor.company.com -u admin -p password

# 从 stdin 读取密码（适合 CI/CD）
echo "password" | hpn login harbor.company.com -u admin --password-stdin

# 使用环境变量
export REGISTRY_USERNAME=admin
export REGISTRY_PASSWORD=password
hpn login harbor.company.com

# 非安全连接（HTTP 或自签名证书）
hpn login http://registry.local -u admin -p password --insecure

# 指定运行时
hpn login harbor.company.com -u admin -p password --runtime podman
```

### 在操作中使用认证（临时）

```bash
# Pull 时使用临时认证（不持久化）
hpn pull -f images.txt -u admin -p password

# Push 时使用临时认证（不持久化）
hpn push -f images.txt --registry harbor.com -u admin -p password
```

## 测试要点

1. **多运行时测试**：验证 Docker/Podman/Skopeo/Nerdctl 的登录功能
2. **认证文件测试**：验证认证信息正确写入各运行时的配置文件
3. **兼容性测试**：验证登录后，原生工具（docker pull/push）能正常使用认证
4. **安全性测试**：验证密码不会出现在日志或错误信息中
5. **非安全连接测试**：验证 `--insecure` 在私有环境中的工作
6. **交互式输入测试**：验证密码输入被正确隐藏
7. **环境变量测试**：验证环境变量认证方式

## 注意事项

1. **安全性**：

   - 密码不应出现在日志中
   - 使用 `-p` 参数时显示警告
   - 认证文件权限设置为 0600

2. **兼容性**：

   - 确保认证文件格式与各运行时兼容
   - 保留现有认证配置，只更新目标仓库

3. **错误处理**：

   - 登录失败时提供清晰的错误信息
   - 区分网络错误、认证错误、配置错误

4. **文档更新**：

   - 更新 README.md 添加 login 使用示例
   - 更新快速开始指南
   - 添加安全最佳实践文档